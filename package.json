{
  "name": "invoice-gen",
  "version": "1.0.0",
  "description": "CLI tool for generating financial documents (invoices, quotations, receipts) as PDF",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "generate": "bun run src/index.ts",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.21",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
